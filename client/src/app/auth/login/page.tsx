import React from "react";
import LoginPage from "./LoginPage";

type Props = {};

const Login = (props: Props) => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default Login;
